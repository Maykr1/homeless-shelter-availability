services:
  frontend:
    container_name: homeless-shelter-availability-frontend
    build:
      context: ./homeless-shelter-availability-web
    ports:
      - "3000:80"
    restart: unless-stopped
