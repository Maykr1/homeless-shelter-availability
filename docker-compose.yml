version: "3.9"

services:
  # frontend:
  #   build: ./homeless-shelter-availability-web
  #   container_name: frontend
  #   ports:
  #     - "3000:3000"
  #   volumes:
  #     - ./frontend:/app
  #   working_dir: /app
  #   depends_on:
  #     - backend

  backend:
    image: deez
    build: 
      context: ./homeless-shelter-availability-api
      dockerfile: Dockerfile
      args:
        - JAR_FILE=target/homeless-shelter-availability-api-0.0.1-SNAPSHOT.jar
    container_name: backend
    ports:
      - "8081:8080"
    depends_on:
      - db
    environment:
      - DB_HOST=db
      - DB_USER=postgres
      - DB_PASSWORD=ZkY6tG5lp!
      - DB_NAME=HSADB

  db:
    image: postgres:15
    container_name: database
    restart: always
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: ZkY6tG5lp
      POSTGRES_DB: HSADB
    ports:
      - "5432:5432"
    volumes:
      - db_data:/var/lib/postgresql/data

volumes:
  db_data: